{
  "name": "@repo/permissions",
  "version": "0.1.0",
  "private": true,
  "description": "The RBAC registry: every permission and role grant, declared once. Layer 0 so both @repo/auth and @repo/authz can read it.",
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test:unit": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@repo/types": "workspace:*"
  },
  "devDependencies": {
    "@repo/tsconfig": "workspace:*",
    "@repo/vitest-config": "workspace:*",
    "@vitest/coverage-v8": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:"
  },
  "repo": {
    "layer": 0,
    "runtime": "browser"
  }
}
