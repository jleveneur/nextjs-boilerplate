{
  "name": "@repo/auth",
  "version": "0.1.1",
  "private": true,
  "description": "Better Auth server config, client, and Actor resolver.",
  "type": "module",
  "exports": {
    ".": "./src/index.ts",
    "./client": "./src/client.ts",
    "./testing": "./src/testing/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test:unit": "vitest run",
    "test:watch": "vitest",
    "test:integration": "vitest run --config vitest.integration.config.ts"
  },
  "dependencies": {
    "@better-auth/api-key": "catalog:",
    "@better-auth/passkey": "catalog:",
    "@repo/types": "workspace:*",
    "@repo/utils": "workspace:*",
    "better-auth": "catalog:",
    "ioredis": "catalog:",
    "server-only": "catalog:"
  },
  "devDependencies": {
    "@repo/authz": "workspace:*",
    "@repo/db": "workspace:*",
    "@repo/tsconfig": "workspace:*",
    "@repo/vitest-config": "workspace:*",
    "@types/node": "catalog:",
    "@vitest/coverage-v8": "catalog:",
    "drizzle-orm": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:"
  },
  "repo": {
    "layer": 1,
    "runtime": "node"
  }
}
