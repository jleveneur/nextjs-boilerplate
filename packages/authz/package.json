{
  "name": "@repo/authz",
  "version": "0.0.0",
  "private": true,
  "description": "Permission registry, RBAC roles, and pure can()/authorize() policy primitives.",
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test:unit": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@repo/errors": "workspace:*",
    "@repo/types": "workspace:*",
    "server-only": "0.0.1"
  },
  "devDependencies": {
    "@repo/tsconfig": "workspace:*",
    "@repo/vitest-config": "workspace:*",
    "@vitest/coverage-v8": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:"
  },
  "repo": {
    "layer": 1,
    "runtime": "node"
  }
}
