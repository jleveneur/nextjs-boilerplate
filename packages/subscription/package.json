{
  "name": "@repo/subscription",
  "version": "1.0.0",
  "private": true,
  "description": "Subscription slice: Stripe checkout, entitlements, and subscription state.",
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test:unit": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@repo/authz": "workspace:*",
    "@repo/db": "workspace:*",
    "@repo/errors": "workspace:*",
    "@repo/kernel": "workspace:*",
    "@repo/permissions": "workspace:*",
    "@repo/types": "workspace:*",
    "drizzle-orm": "catalog:",
    "server-only": "catalog:"
  },
  "devDependencies": {
    "@repo/tsconfig": "workspace:*",
    "@repo/vitest-config": "workspace:*",
    "@types/node": "catalog:",
    "@vitest/coverage-v8": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:"
  },
  "repo": {
    "layer": 3,
    "runtime": "node"
  }
}
