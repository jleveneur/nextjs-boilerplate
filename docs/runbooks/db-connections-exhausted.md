# Stub — database connections exhausted

Symptoms: `too many clients`, API/worker 503, Postgres `pg_stat_activity` near `max_connections`.

See connection budget formula in [deploy.md](./deploy.md). Full runbook: Phase 16.
