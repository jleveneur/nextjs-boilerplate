{
  "extends": "@repo/tsconfig/node.json",
  "compilerOptions": {
    // `@repo/email` re-exports React email templates; typecheck needs JSX.
    "jsx": "react-jsx"
  },
  "include": ["src/**/*.ts", "vitest.config.ts"],
  "exclude": ["node_modules", "dist"]
}
