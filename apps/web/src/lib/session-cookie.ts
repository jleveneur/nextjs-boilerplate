/**
 * Better Auth session cookie name (default product cookie).
 *
 * Used only for presence checks in `proxy.ts` — never for authentication.
 */
export const SESSION_COOKIE_NAME = "better-auth.session_token";
